/**
 * Re-export from @jetpayment/protocol-native.
 * @deprecated Import from '@jetpayment/protocol-native' directly in new code.
 */
export {
  BaseDiscoveryProvider,
  MoltbookDiscoveryProvider,
  DiscoveryService,
  type MoltbookConfig,
} from '../../packages/protocol-native/src/discovery';
export type { IDiscoveryService, DiscoveryProviderConfig } from '../../packages/core/src/types';
